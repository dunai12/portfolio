
mixin title(title, options = {})
    div(class= `title ${options.mainClass || ""}`)
        if title.bg
            +svg_image(title.bg, "title__bg", {viewBox: "0 0 498.161 175.44"})
        div(class= `title__text ${options.text || ""}`)= title.text
